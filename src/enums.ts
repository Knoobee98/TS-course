//enum

// numeric enum
enum Month {
    JAN = 1,
    FEB,
    MAR,
    APR,
    MAY,
    JUN,
    JUL,
    AUG,
    SEP,
    OCT,
    NOV,
    DEC
}

console.log(Month.JAN)

// string enum
enum bulan {
    JAN = "januari",
    FEB = "februari",
    MAR = "maret"
}

console.log(bulan.FEB)